import 'package:flutter/widgets.dart';

// RouteObserver global que permite a las pantallas saber cuándo vuelven a ser visibles
final RouteObserver<PageRoute> routeObserver = RouteObserver<PageRoute>();

